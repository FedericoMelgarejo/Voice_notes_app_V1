<!DOCTYPE html>
<html lang="en" class="h-100">

<head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <title>Document</title>
</head>

<body class="h-100">
    <div class="container h-100">
        <div class="row h-100 justify-content-center align-items-center">
            <div class="col-10 col-md-8 col-lg-6 px-5 py-5" style="border-radius: 5%; background-color: #e8f5e9;">
                <!-- Form -->
                <form method="POST" action="/update/<%= record.id %>?_method=PUT">
                <h1>Update record #<%= record.id %>
                </h1>
                <!-- Input fields -->
                <div class="form-group mb-3">
                    <label for="title">Title:</label>
                    <input type="text" class="form-control" placeholder="Title..." name="title"
                        value="<%= record.title %>">
                </div>
                <div class="form-group mb-3">
                    <label for="addressee">To:</label>
                    <p>
                        <%= record.addressee %>
                    </p>
                    <p>Date: <%= record.createdAt %>
                    </p>

                </div>
                <label for="preview">Preview:</label>
                <% if (record.file !='no_file' ) { %>
                    <div class="d-flex justify-content-center mb-3">
                        <audio controls>
                            <source src="../records/<%= record.file %>" type="audio/ogg" />
                        </audio>
                    </div>
                    <% } else { %>
                        <div class="py-1 mt-1 d-flex justify-content-center">
                            <h2>
                                There is no file
                            </h2>
                        </div>
                        <% } %>
                            <div class="d-flex justify-content-center">
                                <button type="submit" class="btn btn-secondary mx-3">Save</button>
                                <a href="/list" class="btn btn-secondary mx-3">Cancel</a>
                            </div>
                        </form>
                            <!-- Form end -->
            </div>
        </div>

    </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p"
        crossorigin="anonymous"></script>
</body>

</html>